# 🛡️ 에이전트 하네스 마스터 설정

이 폴더와 하위 디렉토리(`.claude/`)는 에이전트의 모든 행동을 통제하는 절대적인 규칙(Rule)과 워크플로우(Skill)의 집합소입니다. 

에이전트는 프롬프트 대화 상의 문맥보다 이곳에 기록된 마크다운 파일들의 규칙을 무조건 우선시해야 합니다. 만약 사용자의 요청이 `architecture.md`나 `agents.md`의 규칙에 위배된다면, 에이전트는 구현을 거부하고 규칙 위반 사실을 사용자에게 경고해야 합니다.

## 코드 작성 전 반드시 확인
* **`architecture.md`, `agents.md`** — 설계 원칙(레이어, 컨벤션, ADR). 사람도 읽는 문서라 쪼개지 않고 하나로 유지합니다.
* **`.claude/guides/feedback.md`** — 과거 Phase에서 지적받았던 것들의 요약. architecture.md 전체를 다시 읽기 전에 여기부터 훑으면 같은 실수를 반복할 확률이 줄어듭니다.
* **`.claude/guides/`** — "접근·운영" 정보(로컬 DB/Kafka/MinIO 접속정보 등)를 상황별로 찾아가는 곳입니다. 설계 결정이 아니라 "지금 뭐가 어디 떠 있는지"라 architecture.md와 분리했습니다.
* **`.claude/tools.md`** — 실제로 쓸 수 있는 도구 목록(빌드/버전관리/로컬인프라/E2E). 여기 없는 도구를 임의로 설치하지 마십시오.
* **`.claude/skills/`, `.claude/rules/`, `.claude/hooks/`** — 언제 무엇을 해야 하는지의 절차서. 상황에 맞는 걸 스스로 찾아 적용하십시오.
