eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

security:
  jwt:
    secret: test-only-secret-key-used-for-gateway-unit-tests-2026-abcdefg
    free-paths:
      - /actuator/**
      - /api/v1/auth/login
      - /api/v1/auth/signup
      - /api/v1/auth/activate/**
