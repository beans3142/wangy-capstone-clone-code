# 🤖 AI 에이전트 행동 강령 (Code of Conduct)

본 프로젝트는 고도의 코드 품질을 요구하는 대규모 마이크로서비스 프로젝트입니다. 에이전트는 어떠한 상황에서도 아래의 강제 수칙을 위반해서는 안 되며, 자율적 판단보다 명시된 룰을 최우선으로 삼아야 합니다.

## 🚨 1. 절대 위반 금지 수칙 (MUST NOT)
* **상상에 의한 코딩 금지:** 일반적인 Spring이나 React 관례를 동원하여 코딩하지 마십시오. 본 프로젝트는 JUnit 4(`@SpringBootTest` 기반 `AbstractServiceTest`), 동적 프로젝션(`Class<T> type`), 커스텀 훅(`useHook`) 로직 분리, MUI v4 등 프로젝트 고유의 뼈대(Boilerplate)를 가집니다. 이를 무시하면 코드는 폐기됩니다.
* **에러 방치 및 덤프 금지:** 한 번에 수많은 파일을 생성하고 수십 개의 컴파일 에러를 방치한 채 작업을 종료하지 마십시오. 한 번에 하나의 파일을 만들고 컴파일 성공 여부를 확인하며 전진해야 합니다.
* **도메인 경계 침범 금지:** 작업 중인 마이크로서비스가 아닌 다른 마이크로서비스의 `Entity`, `Repository`, `Controller`를 `import` 하거나 직접 호출하는 행위는 즉시 반려(Reject) 사유입니다.

## ✅ 2. 강제 이행 수칙 (MUST DO)
* **작업 전 문서 정독:** 어떤 코드를 작성하기 전이든 무조건 `architecture.md`를 먼저 읽어 **실제 레포지토리가 사용하는 정확한 라이브러리 버전과 패턴**을 머릿속에 각인시켜야 합니다.
* **검증 게이트웨이 통과:** 모든 작업은 엄격한 TDD 워크플로우에 따라 터미널에서 단위 테스트 명령어(`mvn test` 또는 `npm run test` 등)를 실행하여 스스로 검증을 거쳐 Green 상태를 확인한 후에만 완료로 인정됩니다.
