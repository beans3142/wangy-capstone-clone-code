# api-gateway 베이스 URL. 비워두면 http://localhost:8080 을 기본값으로 사용한다.
# Vite는 클라이언트에 노출할 환경변수에 VITE_ 접두사를 강제하므로
# PRD가 언급한 CRA식 REACT_APP_API_BASE_URL 대신 이 이름을 쓴다 (worklog 참고).
VITE_API_BASE_URL=http://localhost:8080
